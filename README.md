# curriculum.glitchk.id

Meu currículo atualizado, sempre. :)

HTML estático, sem build e sem dependências, publicado via GitHub Pages.
Para editar, mexa direto no `index.html` (o CSS mora no `<style>` dele).
